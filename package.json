{
  "name": "cardioai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Based Cardiovascular Disease Prediction Website (Node.js + MongoDB + Python ML service)",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix frontend && npm run build",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server"
  }
}
